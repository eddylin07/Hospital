# Project Agent Instructions

## Project

- Name: Hospital
- Stack: Java, Maven, Spring Boot 2.0, MyBatis, Freemarker, MySQL
- Purpose: Hospital / medical information management system.

## Working Notes

- Read `MEMORY.md` before modifying code.
- Keep fixes minimal and focused.
- Prefer existing controller/service/mapper patterns over broad rewrites.
- Do not record credential values. If credentials are relevant, record only the file path where they live.

## Testing

- Use Maven tests when changing Java behavior.
- If Maven is unavailable in the cloud image, install Maven before running tests.
