# MEMORY.md

## Project facts

- Project name: Hospital.
- Stack: Java Maven application using Spring Boot 2.0, MyBatis, Freemarker templates, MySQL connector, PageHelper, Druid, Apache POI, and iText.
- Entry point: `src/main/java/com/hospital/HospitalApp.java`.
- Tests: no `src/test` files were present when this memory was initialized.

## Workflow notes

- Start each task by reading `AGENTS.md` and `MEMORY.md`.
- For critical bug investigations, inspect recent behavioral commits and only fix issues with a concrete high-severity trigger.
- Credentials should be remembered by location only, never by value.

## Unknowns

- Supported runtime Java version is not confirmed.
- Deployment and database migration process are not confirmed.
